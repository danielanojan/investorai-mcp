[build]
builder = "dockerfile"

[deploy]
startCommand = "uv run uvicorn investorai_mcp.server:create_app --factory --host 0.0.0.0 --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
